# Execute

Select exactly one `READY` task. Mark it `IN_PROGRESS`, prepare the delegate handoff, and use Computer Use to control Antigravity when available. After the worker report, compare reported files with `git diff` and mark `VERIFYING`; never mark `DONE` from the report alone. Keep implementation within the task scope and preserve unrelated changes.
