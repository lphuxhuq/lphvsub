# Analyze

Read `AGENTS.md` and existing `.agents/` rules. Do discovery only: inspect Git state, structure, language/framework, build/test/dependency configuration, CI, entry points, relevant execution flow, existing skills/automation, Antigravity executable/process, Computer Use capability, and UI launch path. Separate `OBSERVED`, `HYPOTHESIS`, `VERIFIED`, and `UNKNOWN`. Record findings in `.codex/state/progress.md` (and a project-map artifact when useful). Do not edit product code or delegate to Antigravity during ANALYZE.
