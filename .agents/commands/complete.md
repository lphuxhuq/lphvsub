# Complete

Mark `COMPLETE` only when all required tasks are `DONE`, acceptance criteria and regression checks pass, worker reports and Computer Use control evidence exist where required, UI/UX verification passes or is `NOT_APPLICABLE`, Codex final review is PASS, no blocker remains, and state/evidence files are current. If Computer Use was unavailable, explicitly include `COMPUTER_USE_UNAVAILABLE` and do not claim UI/UX verification. Do not create commits unless requested.
