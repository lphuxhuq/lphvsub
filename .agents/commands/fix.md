# Fix loop

For a FAILED task or failed test/UI check: capture the exact failure, perform root-cause analysis, create `FIX-###` from `templates/fix-task.md`, update dependencies/state, and prepare a handoff containing the failure evidence and acceptance criteria. Control Antigravity through Computer Use when available, then verify and review again. Repeat until PASS or a validated blocker requires user input.
